syntax = "proto3";

package service;

message Request {
    string uid = 1;
}

message Response {
    string msg = 1;
    int64 status = 2;
    string info = 3;
}

service HelloService {
    rpc SayHello (Request) returns (Response);
}

